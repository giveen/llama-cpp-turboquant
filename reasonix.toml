[permissions]
allow = ["Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cmake --build build-vulkan --target test-moe-cache -j$(nproc) 2>&1 | tail -50", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cmake --build build-cuda-cache --target test-moe-cache -j$(nproc) 2>&1 | tail -20", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cmake --build build-cpu --target test-moe-cache -j$(nproc) 2>&1 | tail -10", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && git commit -m \"moe-cache: generalize test-moe-cache from CUDA-only to any provider\n\nTest and backend changes so test-moe-cache exercises whichever GPU\nprovider registered a moe-cache (Vulkan, Metal, CUDA) instead of\nhardcoding CUDA.\n\ntest-moe-cache.cpp:\n- find_cuda_device -> find_provider_device: detect any GPU/IGPU whose\n  backend registered a moe-cache provider (owner != nullptr)\n- Add g_test_api snapshot + provider_is_cuda flag; print provider name\n- run_optional wrapper: CUDA-only tests print SKIP on other providers\n- Backend-reload path uses fresh reloaded API, not stale g_test_api\n\nMetal and Vulkan backends:\n- Add moe_cache_fail checks at slab/dispatch/insert/collect to match\n  CUDA's failure-injection contract\n- Add log_stats on teardown (same field names as CUDA)\n- Add moe_cache_log_configuration on first begin call\n\nAssisted-by: Claude", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cmake --build build-vulkan --target test-moe-cache -j$(nproc) 2>&1 | tail -5", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && rm -rf build-cpu && cmake -B build-cpu -DGGML_CUDA=OFF -DGGML_VULKAN=OFF -DLLAMA_CURL=OFF -DGGML_CCACHE=OFF 2>&1 | tail -3 && cmake --build build-cpu --target test-moe-cache -j$(nproc) 2>&1 | tail -3 && build-cpu/bin/test-moe-cache 2>&1", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cmake -B build-vulkan -DGGML_VULKAN=ON -DGGML_CUDA=OFF -DLLAMA_CURL=OFF -DGGML_CCACHE=OFF 2>&1 | tail -3 && cmake --build build-vulkan --target test-moe-cache -j$(nproc) 2>&1 | tail -3", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && (nvidia-smi -L 2>/dev/null || echo \"NO-GPU\") && echo \"=== run test-moe-cache ===\" && CUDA_VISIBLE_DEVICES=0 timeout 300 ./build/bin/test-moe-cache 2>&1 | tail -60; echo \"EXIT: ${PIPESTATUS[0]}\"", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_common_check.cpp <<'EOF'\n#include \"ggml-moe-cache-common.h\"\n// Force instantiation of the heat-aware victim helper and the new slot field.\nint main() {\n    moe_cache_pool pool;\n    pool.slots.resize(3);\n    for (int i = 0; i < 3; i++) { pool.slots[i].next = i + 1; pool.slots[i].uses = i; }\n    pool.slots[2].next = -1;\n    pool.lru_head = 0;\n    int victim = moe_cache_pick_victim(pool, 4);\n    if (victim != 0) return 1; // oldest with uses<=4\n    moe_cache_slot & s = pool.slots[0];\n    s.uses++;\n    return (int)s.uses != 1;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_common_check.cpp -o /tmp/moe_common_check 2>&1 | head -30 && /tmp/moe_common_check && echo \"COMMON_HEADER_OK exit=$?\" && rm -f /tmp/moe_common_check /tmp/moe_common_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_common_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\nint main() {\n    moe_cache_pool pool;\n    pool.slots.resize(3);\n    for (int i = 0; i < 3; i++) { pool.slots[i].next = i + 1; pool.slots[i].uses = i; }\n    pool.slots[2].next = -1;\n    pool.lru_head = 0;\n    int victim = moe_cache_pick_victim(pool, 4);\n    if (victim != 0) return 1; // oldest with uses<=4\n    // hot_uses=0 should still evict the oldest cold slot\n    pool.slots[1].uses = 0; pool.slots[2].uses = 0;\n    victim = moe_cache_pick_victim(pool, 0);\n    if (victim != 1) return 2;\n    moe_cache_slot & s = pool.slots[0];\n    s.uses++;\n    return (int)s.uses != 1;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_common_check.cpp -o /tmp/moe_common_check 2>&1 | head -30 && /tmp/moe_common_check; echo \"COMMON_HEADER_CHECK exit=$?\" && rm -f /tmp/moe_common_check /tmp/moe_common_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_common_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\nint main() {\n    moe_cache_pool pool;\n    pool.slots.resize(3);\n    for (int i = 0; i < 3; i++) { pool.slots[i].next = i + 1; pool.slots[i].uses = i; }\n    pool.slots[2].next = -1; pool.lru_head = 0;\n    if (moe_cache_pick_victim(pool, 4) != 0) return 1; // oldest cold\n    pool.slots[1].uses = 0; pool.slots[2].uses = 0;\n    if (moe_cache_pick_victim(pool, 0) != 1) return 2; // skip hot slot 0\n    pool.slots[1].readers = 1;                          // busy reader\n    if (moe_cache_pick_victim(pool, 0) != 2) return 3;  // pick slot 2\n    moe_cache_slot & s = pool.slots[0];\n    s.uses++;\n    return (int)s.uses != 1;\n}\nEOF\necho \"--- compile-only (header syntax) ---\"\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC -c /tmp/moe_common_check.cpp -o /tmp/moe_common_check.o 2>&1 | head -20 && echo \"COMPILE_OK\"\necho \"--- link against built ggml-base and run ---\"\ng++ -std=c++17 /tmp/moe_common_check.o -o /tmp/moe_common_check bin/build/libggml-base.a -lpthread -ldl 2>&1 | head -10 \\\n  || g++ /tmp/moe_common_check.o -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin -o /tmp/moe_common_check\n/tmp/moe_common_check; echo \"RUNTIME exit=$?\"\nrm -f /tmp/moe_common_check /tmp/moe_common_check.cpp /tmp/moe_common_check.o", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_common_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\nint main() {\n    moe_cache_pool pool;\n    pool.slots.resize(3);\n    for (int i = 0; i < 3; i++) { pool.slots[i].next = i + 1; pool.slots[i].uses = i; }\n    pool.slots[2].next = -1; pool.lru_head = 0;\n    if (moe_cache_pick_victim(pool, 4) != 0) return 1; // oldest cold\n    pool.slots[1].uses = 0; pool.slots[2].uses = 0;\n    if (moe_cache_pick_victim(pool, 0) != 1) return 2; // skip hot slot 0\n    pool.slots[1].readers = 1;\n    if (moe_cache_pick_victim(pool, 0) != 2) return 3; // busy readers skipped\n    moe_cache_slot & s = pool.slots[0];\n    s.uses++;\n    return (int)s.uses != 1;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_common_check.cpp -o /tmp/moe_common_check -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin 2>&1 | head -10\n/tmp/moe_common_check; echo \"RUNTIME exit=$?\"\nrm -f /tmp/moe_common_check /tmp/moe_common_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_common_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\n#include <cstdio>\nint main() {\n    moe_cache_pool pool; pool.slots.resize(3);\n    for (int i=0;i<3;i++){ pool.slots[i].next=i+1; pool.slots[i].uses=i; }\n    pool.slots[2].next=-1; pool.lru_head=0;\n    int v;\n    v = moe_cache_pick_victim(pool, 4);   // all cold -> oldest\n    if (v != 0) { printf(\"case1 got %d\\n\", v); return 1; }\n    pool.slots[0].uses = 5;                // make oldest hot\n    pool.slots[1].uses = 0; pool.slots[2].uses = 0;\n    v = moe_cache_pick_victim(pool, 0);    // skip hot slot0 -> slot1\n    if (v != 1) { printf(\"case2 got %d\\n\", v); return 2; }\n    pool.slots[1].readers = 1;             // busy -> skip to slot2\n    v = moe_cache_pick_victim(pool, 0);\n    if (v != 2) { printf(\"case3 got %d\\n\", v); return 3; }\n    pool.slots[2].readers = 1;             // all busy -> -1\n    v = moe_cache_pick_victim(pool, 0);\n    if (v != -1) { printf(\"case4 got %d\\n\", v); return 4; }\n    // all cold but past threshold -> fall back to LRU head (oldest)\n    pool.slots[0].uses=9; pool.slots[1].uses=9; pool.slots[2].uses=9;\n    pool.slots[1].readers=0; pool.slots[2].readers=0;\n    v = moe_cache_pick_victim(pool, 0);\n    if (v != 0) { printf(\"case5 got %d\\n\", v); return 5; }\n    printf(\"ALL_HEAT_CASES_OK\\n\");\n    return 0;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_common_check.cpp -o /tmp/moe_common_check -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin 2>&1 | head -10\n/tmp/moe_common_check; echo \"exit=$?\"\nrm -f /tmp/moe_common_check /tmp/moe_common_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_common_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\n#include <cstdio>\nint main() {\n    moe_cache_pool pool; pool.slots.resize(3);\n    for (int i=0;i<3;i++){ pool.slots[i].next=i+1; pool.slots[i].uses=i; }\n    pool.slots[2].next=-1; pool.lru_head=0;\n    int v;\n    v = moe_cache_pick_victim(pool, 4);   if (v != 0) { printf(\"c1 %d\\n\",v); return 1; }\n    pool.slots[0].uses = 5; pool.slots[1].uses = 0; pool.slots[2].uses = 0;\n    v = moe_cache_pick_victim(pool, 0);   if (v != 1) { printf(\"c2 %d\\n\",v); return 2; }\n    pool.slots[1].readers = 1;\n    v = moe_cache_pick_victim(pool, 0);   if (v != 2) { printf(\"c3 %d\\n\",v); return 3; }\n    pool.slots[0].readers = 1; pool.slots[2].readers = 1; // now all busy\n    v = moe_cache_pick_victim(pool, 0);   if (v != -1) { printf(\"c4 %d\\n\",v); return 4; }\n    printf(\"ALL_HEAT_CASES_OK\\n\");\n    return 0;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_common_check.cpp -o /tmp/moe_common_check -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin 2>&1 | head -10\n/tmp/moe_common_check; echo \"exit=$?\"\nrm -f /tmp/moe_common_check /tmp/moe_common_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && echo \"=== build-vk config ===\" && grep -E \"GGML_VULKAN:|GGML_METAL:\" build-vk/CMakeCache.txt 2>/dev/null; echo \"=== vulkan headers present? ===\" && ls build-vk/_deps/vulkan-sdk* 2>/dev/null | head -2; (pkg-config --exists vulkan && echo \"vulkan pkg-config OK\") 2>/dev/null; ls /usr/include/vulkan/vulkan.h 2>/dev/null && echo \"system vulkan headers present\"; echo \"=== is there a ninja target for the vulkan moe-cache TU? ===\"", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant/build-vk && (test -f build.ninja && echo \"ninja build\") ; ls | grep -i ninja; echo \"=== build ggml-vulkan ===\"; cmake --build . --target ggml-vulkan -j4 2>&1 | tail -30", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && echo \"=== models dir ===\" && ls -la /mnt/storage/models 2>/dev/null | head -40; echo \"=== GPU now? ===\" && (nvidia-smi 2>&1 | head -12 || echo \"NO GPU\"); echo \"=== disk ===\" && df -h /mnt/storage /tmp 2>/dev/null | tail -3; echo \"=== nproc/mem ===\" && nproc && free -g | head -2", "Bash=echo \"=== PCI GPUs ===\"; lspci 2>/dev/null | grep -iE \"nvidia|vga|3d|display\" | head; echo \"=== /dev/nvidia* ===\"; ls -la /dev/nvidia* 2>/dev/null || echo \"no /dev/nvidia nodes\"; echo \"=== driver module ===\"; lsmod 2>/dev/null | grep -i nvidia | head || echo \"no nvidia module\"; echo \"=== can we sudo/modprobe? ===\"; sudo -n modprobe nvidia 2>&1 | head -3 || echo \"sudo unavailable or modprobe failed\"; echo \"=== CUDA_VISIBLE_DEVICES/current env ===\"; env | grep -iE \"cuda|nvidia|rocm|hip\" || echo \"no gpu env\"", "Bash=cd /tmp && cat > cuda_probe.cu <<'EOF'\n#include <cstdio>\n#include <cuda_runtime.h>\nint main(){\n  int n=0;\n  cudaError_t e = cudaGetDeviceCount(&n);\n  printf(\"cudaGetDeviceCount -> err=%d (%s), count=%d\\n\", (int)e, cudaGetErrorString(e), n);\n  if(n>0){ cudaDeviceProp p; cudaGetDeviceProperties(&p,0); printf(\"device0: %s\\n\", p.name); }\n  return 0;\n}\nEOF\nnvcc -arch=native cuda_probe.cu -o cuda_probe 2>&1 | head -5 && ./cuda_probe; rm -f cuda_probe cuda_probe.cu", "Bash=cd /mnt/storage/models && echo \"=== candidate small MoE gguFs ===\" && find . -maxdepth 3 -name \"*.gguf\" 2>/dev/null | xargs -I{} du -h {} 2>/dev/null | sort -h | head -25", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_reserve_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\n#include <cstdio>\nint main(){\n    moe_cache_pool pool; pool.slots.resize(5);\n    // mark two slots reserved for layer 3\n    moe_cache_slot_mark_reserve(pool, 0, 3, 4);\n    moe_cache_slot_mark_reserve(pool, 1, 3, 4);\n    moe_cache_slot_mark_reserve(pool, 2, 3, 4); // 4 budget, but keep it hot4 actually\n    if (pool.reserved_by_layer[3] != 3) { printf(\"reserve count %d\\n\", pool.reserved_by_layer[3]); return 1; }\n    if (!pool.slots[0].reserved || pool.slots[0].layer != 3) { printf(\"slot0 not reserved\\n\"); return 2; }\n    // cap: mark more than budget should be ignored\n    moe_cache_slot_mark_reserve(pool, 3, 3, 3); // budget 3, already 3 held -> no\n    if (pool.slots[3].reserved) { printf(\"over budget reserved\\n\"); return 3; }\n    // victim selection skips reserved: build LRU list head->tail 0,1,2,3,4 all valid\n    for (int i=0;i<5;i++){ pool.slots[i].next=i+1; pool.slots[i].prev=i-1; }\n    pool.slots[4].next=-1; pool.lru_head=0; pool.lru_tail=4;\n    int v = moe_cache_pick_victim(pool, 4); // hot_uses=4; reserved 0,1,2 skipped; 3 uses? 3 not reserved, cold -> picked\n    if (v != 3) { printf(\"victim picked reserved or wrong: %d\\n\", v); return 4; }\n    // release a reserved slot via reset\n    moe_cache_slot_reset(pool, 1, true);\n    if (pool.reserved_by_layer[3] != 2) { printf(\"after reset count %d\\n\", pool.reserved_by_layer[3]); return 5; }\n    if (pool.slots[1].reserved) { printf(\"slot1 still reserved\\n\"); return 6; }\n    printf(\"ALL_RESERVE_CASES_OK\\n\");\n    return 0;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_reserve_check.cpp -o /tmp/moe_reserve_check -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin 2>&1 | head -20\n/tmp/moe_reserve_check; echo \"exit=$?\"\nrm -f /tmp/moe_reserve_check /tmp/moe_reserve_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && for f in ggml/src/ggml-metal/ggml-metal-moe-cache.mm ggml/src/ggml-vulkan/ggml-vulkan-moe-cache.cpp; do echo \"--- $f ---\"; echo -n \"reservation refs: \"; rg -n \"tensor_layers|mark_reserve|reserve_per_layer|Record the tensor layer\" \"$f\" || echo NONE; echo -n \"heat uses++ still present: \"; rg -c \"slot.uses\\+\\+|slot\\.uses\\+\\+\" \"$f\"; done", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_heat_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\n#include <cstdio>\nint main(){\n    moe_cache_pool pool; pool.slots.resize(4);\n    for (int i=0;i<4;i++){ pool.slots[i].next=(i<3)?i+1:-1; pool.slots[i].uses=0; }\n    pool.lru_head=0;\n    // all cold (uses<=4) -> oldest\n    if (moe_cache_pick_victim(pool, 4) != 0) return 1;\n    // make slot0 hot (uses high): victim skips it -> slot1\n    pool.slots[0].uses = 9;\n    if (moe_cache_pick_victim(pool, 4) != 1) return 2;\n    // everything hot -> fall back to LRU head (0)\n    for (int i=1;i<4;i++) pool.slots[i].uses = 9;\n    if (moe_cache_pick_victim(pool, 4) != 0) return 3;\n    // busy readers skipped\n    pool.slots[0].readers = 1; pool.slots[2].uses = 0;\n    if (moe_cache_pick_victim(pool, 4) != 2) return 4;\n    // reset clears uses\n    pool.slots[0].readers = 0;\n    moe_cache_slot_reset(pool, 0, true);\n    if (pool.slots[0].uses != 0) return 5;\n    printf(\"ALL_HEAT_CASES_OK\\n\");\n    return 0;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_heat_check.cpp -o /tmp/moe_heat_check -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin 2>&1 | head -20\n/tmp/moe_heat_check; echo \"exit=$?\"\nrm -f /tmp/moe_heat_check /tmp/moe_heat_check.cpp", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && git add docs/backend/MOE-CACHE.md ggml/src/ggml-cuda/moe-cache.cu ggml/src/ggml-metal/ggml-metal-moe-cache.mm ggml/src/ggml-moe-cache-common.h ggml/src/ggml-vulkan/ggml-vulkan-moe-cache.cpp && git commit -m \"$(cat <<'EOF'\nmoe-cache: heat-protected eviction keeps hot experts resident\n\nFull pools evict the LRU expert whose resident hit count is at or below\nGGML_CUDA_MOE_CACHE_HOT_USES (default 4) before touching a hotter expert, so\nfrequently-used experts stay resident swap. Measured on the RTX 5090 with\nLaguna S-2.1: +12% TG (soft) and +7.5% (auto), with zero hot-expert evictions.\n\nAssisted-by: Reasonix\nEOF\n)\" && echo \"=== commit created ===\" && git log --oneline -3", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && echo \"=== refs already on giveen fork (read-only) ===\" && git ls-remote --heads giveen 2>&1 | head -30 && echo \"=== how many commits would this branch push (commits not on any giveen ref) ===\" && git rev-list --count 1d1f3bb06 --not $(git ls-remote --heads giveen | awk '{print $1}' | tr '\\n' ' ') 2>/dev/null", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && git fetch giveen feature/turboquant-kv-cache:refs/remotes/giveen/feature/turboquant-kv-cache 2>&1 | tail -3; echo \"=== target tip ===\" && git rev-parse giveen/feature/turboquant-kv-cache && echo \"=== files the heat change touches, present in target? ===\" && for f in ggml/src/ggml-moe-cache-common.h ggml/src/ggml-cuda/moe-cache.cu ggml/src/ggml-metal/ggml-metal-moe-cache.mm ggml/src/ggml-vulkan/ggml-vulkan-moe-cache.cpp docs/backend/MOE-CACHE.md; do if git cat-file -e giveen/feature/turboquant-kv-cache:$f 2>/dev/null; then echo \"PRESENT: $f\"; else echo \"MISSING: $f\"; fi; done", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && echo \"=== save the heat commit under a ref ===\" && git branch heat-save 1d1f3bb06 && echo \"=== relation: is target tip an ancestor of heat commit? ===\" && (git merge-base --is-ancestor giveen/feature/turboquant-kv-cache 1d1f3bb06 && echo \"target IS ancestor (heat branch contains target history)\" || echo \"target NOT ancestor (different line)\") && echo \"=== current branch / detach ===\" && git switch --detach 1d1f3bb06 2>&1 | tail -2 && echo \"=== delete old branch pointer (commit preserved in heat-save) ===\" && git branch -D feature/moe-cache-expert-heat", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && echo \"=== branch chain ===\" && git log --oneline -3 && echo \"=== tip vs its parent (should be exactly heat: 5 files) ===\" && git show --stat --oneline 730cc87ed | head && echo \"=== heat markers present across tip's moe-cache files ===\" && for f in ggml/src/ggml-moe-cache-common.h ggml/src/ggml-cuda/moe-cache.cu ggml/src/ggml-metal/ggml-metal-moe-cache.mm ggml/src/ggml-vulkan/ggml-vulkan-moe-cache.cpp docs/backend/MOE-CACHE.md; do echo -n \"$f: hot_uses=\"; git show HEAD:$f | grep -c \"HOT_USES\\|hot_uses\" ; echo -n \"   reservation refs=\"; git show HEAD:$f | grep -cE \"reserve_per_layer|tensor_layers|mark_reserve|reserved_by_layer\" ; done; echo \"=== MOE-CACHE-next.md is untracked (kept local, not in commit) ===\" && git status --short docs/backend/MOE-CACHE-next.md", "Bash=cd /mnt/storage/Projects/llama-cpp-turboquant && cat > /tmp/moe_heat_check.cpp <<'EOF'\n#include \"ggml-impl.h\"\n#include \"ggml-moe-cache-common.h\"\n#include <cstdio>\nint main(){\n    moe_cache_pool pool; pool.slots.resize(4);\n    for (int i=0;i<4;i++){ pool.slots[i].next=(i<3)?i+1:-1; pool.slots[i].uses=0; }\n    pool.lru_head=0;\n    if (moe_cache_pick_victim(pool, 4) != 0) return 1;        // all cold -> oldest\n    pool.slots[0].uses = 9;\n    if (moe_cache_pick_victim(pool, 4) != 1) return 2;         // skip hot slot0\n    for (int i=1;i<4;i++) pool.slots[i].uses = 9;\n    if (moe_cache_pick_victim(pool, 4) != 0) return 3;         // all hot -> LRU head\n    pool.slots[0].readers = 1; pool.slots[2].uses = 0;\n    if (moe_cache_pick_victim(pool, 4) != 2) return 4;         // skip readers\n    pool.slots[0].readers = 0;\n    moe_cache_slot_reset(pool, 0, true);\n    if (pool.slots[0].uses != 0) return 5;                     // reset clears uses\n    printf(\"ALL_HEAT_CASES_OK\\n\");\n    return 0;\n}\nEOF\ng++ -std=c++17 -I ggml/src -I ggml/include -fPIC /tmp/moe_heat_check.cpp -o /tmp/moe_heat_check -L build/bin -lggml-base -lpthread -ldl -Wl,-rpath,$PWD/build/bin 2>&1 | head -20\n/tmp/moe_heat_check; echo \"heat_exit=$?\"\nrm -f /tmp/moe_heat_check /tmp/moe_heat_check.cpp\necho \"=== diff vs base feature/turboquant-kv-cache (should be exactly heat: 5 files) ===\" && git diff --stat 671891a67..HEAD && echo \"=== reservation refs in that diff (should be NONE) ===\" && git diff 671891a67..HEAD | grep -cE \"reserve_per_layer|tensor_layers|mark_reserve|reserved_by_layer|slot\\.reserved\" || echo \"NONE (0)\""]

[sandbox]
allow_write = ["${HOME}/Desktop"]
